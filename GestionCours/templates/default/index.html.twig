
{% block body %}

    <div class="example-wrapper">
        <h1>Symfony QR-Code ✅</h1> <hr>

        <div class="row">

            <div class="col-md-6">
                {% if qrCode %}
                    <img src="{{ qrCode }}" alt="qr-code">
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}